name: Build and Push Container Image

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: docker-build
    steps:
      - name: Install dependencies
        run: apk add --no-cache docker-cli git

      - name: Checkout
        run: |
          git clone --depth 1 http://git.stahmer.lan/bullitt/trackbox.git /tmp/trackbox
          cd /tmp/trackbox

      - name: Login to Forgejo Container Registry
        run: echo "${{ secrets.REGISTRY_TOKEN }}" | docker login git.stahmer.lan -u bullitt --password-stdin

      - name: Build and push
        run: |
          cd /tmp/trackbox
          docker build -t git.stahmer.lan/bullitt/trackbox:latest .
          docker push git.stahmer.lan/bullitt/trackbox:latest

      - name: Cleanup
        run: rm -rf /tmp/trackbox
